import appConfig from './app.config'
import { loggerConfig } from './logger.config'
import rewardConfig from './reward.config'

export const globalConfig = [appConfig, rewardConfig]
export { loggerConfig }
